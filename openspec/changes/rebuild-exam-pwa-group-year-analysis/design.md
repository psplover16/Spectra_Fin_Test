## Context

????????????????????????? A ??B ??????????? A ??????? 114 ?????????? `src/modules/exam/data/subjectContent.ts` ??????????????????????????????????????? PWA ?????? `vite-plugin-pwa` ?????

## Goals / Non-Goals

**Goals:**

- ?? `/a-group`?`/b-group`?`/language` ? `/a-group/:year` ???
- ?????? redirect ???????invalid year ??? NotFound?
- ? N5 ???? section header ????????????? checkbox?
- ?? A ? 114 ? 50 ???????? lazy loader?
- 107 ? 113 ?????????????
- ???? `vite-plugin-pwa`?`src/app/pwa.ts` ??????? wrapper?
- ????? CI/CD ? PWA output ?????? lint ? PWA plugin build ???

**Non-Goals:**

- ?????? 107 ? 113 ??????
- ?????? B ?????????
- ?????????????? analytics?
- ????? service worker ???
- ???????????????????????????????

## Decisions

### Use group routes as the new public navigation contract

`/a-group` ???????? redirect ? `/a-group`?`/computer-principles` ? `/networking` redirect ? `/a-group`?`/information-management` ? `/programming` redirect ? `/b-group`?`/language` ????????????????????????????????????????????? A ??B ????????

### Treat valid unfinished years differently from invalid years

`/a-group/107` ? `/a-group/113` ??????????????????????`/a-group/999`?`/a-group/abc`?`/a-group/115` ? invalid year ??? NotFound?????????? `/a-group`?????????????????????????

### Create an examGroups feature module instead of extending subjectContent

?? `src/modules/examGroups/aGroup/` ?????????????`subjectContent.ts` ??????????????????????????????????? `subjectContent.ts`??????????????????????????????

### Use localStorage snapshots for first-batch progress

?????????????????????? localStorage snapshot??? shape ??`completedYears: string[]`?`bookmark: { year: string; updatedAt: string } | null`?`version: 1`?`updatedAt: string`?Pinia ???????????????????????? localStorage ???IndexedDB ????????? 114 ????????? TypeScript ????????????????

### Use lazy-loaded static year modules for question content

`yearSummaries.ts` ?? 114 ? 107 ????`useAGroupYearQuestions` ? route year lazy import ??????114 ??? `years/114.ts`?107 ? 113 ??? placeholder ????????????????? A ???????? 400 ??????? bundle ????????

### Convert src/app/pwa.ts into a vite-plugin-pwa update wrapper

Vite ?? `VitePWA` ?? manifest?service worker ? Workbox ?????`src/app/pwa.ts` ?????? `navigator.serviceWorker.register`?????? plugin update prompt ????? ready?unsupported?error ? updateAvailable???????? service worker???? plugin ????????? CI ?? manifest ? service worker output?

### Keep the first batch focused on 114 while preserving the 107 to 113 path

????? 114 ????? 50 ???? 107 ? 113 ?????????????????????? 400 ??????????????PWA?????????????????????

## Implementation Contract

**Routing behavior:** ????? `/a-group`????????? redirect?`/a-group/114` ?????????`/a-group/107` ? `/a-group/113` ???????invalid year ?? NotFound??????? Vue Router route table ? guard ??????? component ? e2e ?????

**A ????? behavior:** `/a-group` ?? 114?113?112?111?110?109?108?107 ??????????????????? checkbox ??????????????????????reload ????????????

**Question analysis data shape:** `ExamQuestionAnalysis` ?? `acceptedAnswers: ('A' | 'B' | 'C' | 'D')[]`?`answerNote: string | null`?`answerVerification`?`originalStem`?`options`?`coreTerms`?`beginnerExplanation`?`solvingSteps`?`optionExplanations`?`keyTakeaways`?`tags`?`sourceRef`??????????????????????????? verified?

**114 content contract:** 114 ??????? 50 ??????????????????????????????????????????????tags ??? PDF???????????????????

**PWA contract:** `vite.config.ts` ?? `VitePWA`?build ???? `dist/manifest.webmanifest` ? service worker?`src/app/pwa.ts` ????? service worker???? plugin update prompt ? PWA ?????CI ??? PWA output?lint?typecheck?unit?build?e2e?

**Scope boundaries:** ? change ??? 114 ??????107 ? 113 ????????????????PWA plugin runtime ? CI/CD?107 ? 113 ???????B ?????????????????????? change?

## Risks / Trade-offs

- 114 ? 50 ????? ? ?????????????????????????????????
- PDF ??????? ? ?????????? extractionStatus??????????????
- PWA plugin ???? ? ? design ?????CI ?? output???? chunk 500 KB ????
- ?? N5 ???????? ? ???????????????????????
- ??? redirect ???????? ? ??????? router tests ? e2e?

## Migration Plan

1. ????????? redirect??? NotFound?
2. ?? A ??????storage ??????
3. ?? 114 ??????loader ??? rendering?
4. ?? `vite-plugin-pwa` ??? `src/app/pwa.ts`?
5. ?? CI/CD ????
6. ? PWA plugin ?? build ? service worker ??????????????????????? `VitePWA` plugin ?? CI PWA output ?????????

## Open Questions

- ?? repository slug ?? `finPubTest`??????? repository ???????? CD base path?
- 107 ? 113 ??????????????????? 114 ??????????
